#ifndef _NumClass
#define _NumClass
#include<stdio.h>
#include<math.h>
int isPrime(int n);//return if the number is prime number
int isStrong(int n);//return if the number is Strong number
int isArmStrong(int n);//return if the numberis ArmStrong number
int isPalindrome(int n);//return if the number is Palindrome
#endif
